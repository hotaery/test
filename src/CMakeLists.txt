FILE(GLOB SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/*.cpp)
ADD_EXECUTABLE(test ${SOURCES})
FOREACH(CPP_SOURCE ${SOURCES})
    GET_FILENAME_COMPONENT(CPP_SOURCE_WE ${CPP_SOURCE} NAME_WE)
    MESSAGE(STATUS "wangziliang ${CPP_SOURCE_WE} ${CPP_SOURCE}")
ENDFOREACH()

#ADD_SUBDIRECTORY(filesystem)
#ADD_SUBDIRECTORY(time)
ADD_SUBDIRECTORY(data_structure)
SET(LIBS data_structure pthread)
TARGET_LINK_LIBRARIES(test ${LIBS})
INSTALL(TARGETS test RUNTIME DESTINATION ../)
